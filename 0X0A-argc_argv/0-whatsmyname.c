#include <stdio.h>
/**
 * main - entry point
 * @argc: an int
 * @argv: pointer to a list
 * Return: 0(success)
 */
int main(int argc, char const *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
